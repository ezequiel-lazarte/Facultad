#include <iostream>
using namespace std;

int main() {
	/// metodos para los string
	/// este metodo 
	length(); /// devuelve la longitud de la cadena en bytes "Ezequiel" tiene 8 bytes
	
	/// el metodo sort ordena un vector de cadenas de string
	vector<string> v = { gta, los santos, vice city, orlando, florida, santa fe };
	sort(v.begin(),v.end()); /// esto ordena el vector
	
	empty(); /// este metodo devuelve true si la cadena esta vacia y false si tiene datos
	find("="); /// busca un caracter o cadena dentro de la cadena y devuelve la pos del mismo
	clear(); /// este borra el contemido de la cadena
	return 0;
}

