#include <iostream>
using namespace std;

int main() {
//	Explique el significado de las siguientes expresiones de código C++.
	
///	a) int a = 25; 
	//	Se declara a "a" de tipo entero y se la inicializa en 25
///	b) int *k = &a;
	//	Se declara el puntero "k" y se le inicializa con la dirrecion de la variable "a"
///	c) double *v1[30];
	//	Se declara un puntero reservando 30 lugares en el stack de tipo double
///	d) double (*v2)[20];
	//	Se declara un puntero de tipo double reservando 20 lugares en el heap
///	e) float f(int *a, int *b);
	//	Se declara una funcion que retorna un dato de tipo float y contiene dos argumentos que son dos punteros
///	f) float (*pf)(int *a, int *b);
	//	Se declara una funcion que retorna un puntero de tipo float y contiene dos argumentos que son dos punteros
	
	return 0;
}

